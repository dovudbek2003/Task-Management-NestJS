export enum RoleEnum {
    SUPER_ADMIN = 'superAdmin',
    ADMIN = 'admin',
    MANAGER = 'manager',
    WORKER = 'worker'
}

export enum StatusEnum {
    TOOK = 'took',
    PROCESS = 'process',
    DONE = 'done',
}